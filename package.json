{
  "name": "llm-impl",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"bun run dev:server\" \"bun run dev:web\"",
    "dev:server": "bun --hot apps/server/src/index.ts",
    "dev:web": "bun run --cwd apps/web dev",
    "build:web": "bun run --cwd apps/web build",
    "typecheck": "tsc -p packages/debug-sdk/tsconfig.json && tsc -p apps/server/tsconfig.json && tsc -p apps/web/tsconfig.json"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.2",
    "@types/bun": "^1.1.14"
  }
}
